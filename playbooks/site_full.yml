# =====================================================================
# Full Topic-1 end-to-end flow (infra + configure + failover validation)
# =====================================================================
# Requires a licensed IRIS container image + iris.key. See
# docs/licensed-image-setup.md before running.
#
# Run:
#   ansible-playbook playbooks/site_full.yml -i inventories/poc \
#     -e iris_key_source=/path/to/license.ISCkey \
#     -e iris_image=containers.intersystems.com/intersystems/iris:latest-cd
- import_playbook: prepare.yml
- import_playbook: stack_up.yml
- import_playbook: verify.yml
- import_playbook: configure.yml
- import_playbook: update_haproxy_primary.yml
- import_playbook: validate_failover_production.yml
- import_playbook: test_routing.yml
