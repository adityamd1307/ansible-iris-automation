---
# Deprecated umbrella — import/register on primary, then auto-start on primary
# after setup_mirror.yml. Prefer the split playbooks in configure.yml.
- import_playbook: setup_production_import.yml
- import_playbook: setup_production_autostart.yml
